#spring:
#  cloud:
#    config:
#      server:
#        native:
#          search-locations: classpath:/shared
#  profiles:
#    active: native
spring:
  cloud:
    config:
      server:
        default-label: dev
        git:
          uri: https://github.com/homewin21/gitRository.git
          search-paths:
            - gitRository
server:
  port: 8888
